```json
{
  "name": "serve-sync-pos-backend",
  "version": "1.0.0",
  "description": "Backend API for Serve Sync POS Landing Page",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}

```
